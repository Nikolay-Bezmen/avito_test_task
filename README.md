## Способ 1: Использование класического токенайзера и русского BERT'a
# Восстановление Пробелов в Тексте с Помощью RuBERT-tiny2

----
Сразу можно сказать очевидную проблему такого подхода : невозможно будет приблизиться к оптимуму так как токенизатор может изначально разбить входную строку на токены таким образом что символ после которого должен был быть пробел будет где-то в середине слова(токена).
----
Этот проект представляет собой решение задачи по восстановлению пропущенных пробелов в слитном русском тексте. Основная идея заключается в том, чтобы переформулировать задачу как проблему **классификации токенов** (Token Classification).

## 🚀 Основной подход

Каждый токен, полученный из слитного текста, классифицируется по двум категориям:
*   **`BEGIN` (метка `1`)**: Этот токен является началом нового слова. Перед ним нужно поставить пробел (кроме самого первого слова).
*   **`CONTINUE` (метка `0`)**: Этот токен является продолжением предыдущего слова. Пробел перед ним не нужен.

Для решения этой задачи была дообучена легковесная трансформерная модель **`cointegrated/rubert-tiny2`**.

## 🔧 Ключевые методы и решения

В ходе работы над проектом были применены следующие техники для достижения высокого качества и стабильности модели:

### 1. Выравнивание меток при токенизации (`Token-Label Alignment`)

Это одна из ключевых сложностей задачи. Стандартный токенизатор (например, WordPiece) разбивает слова на саб-токены (sub-tokens). Например, слово `playstation` может превратиться в `['play', '##station']`. Необходимо было разработать механизм, который корректно назначит метку `BEGIN` только первому саб-токену (`play`), а всем остальным — `CONTINUE` (`##station`).

**Решение:**
1.  **Создание карты на уровне символов:** Для каждого текста без пробелов создавался "идеальный" массив меток (`char_labels`), где `1` помечался только тот символ, с которого начинается новое слово.
2.  **Использование `offset_mapping`:** При токенизации запрашивалась карта смещений (`offset_mapping`), которая связывает каждый токен с его позицией в исходной символьной строке.
3.  **Перенос меток:** Метка токена определялась по метке его **первого символа** в `char_labels`. Это позволило безошибочно выровнять метки даже при сложном дроблении слов.

### 2. Борьба с дисбалансом классов

В задаче наблюдается сильный дисбаланс: токенов с меткой `CONTINUE` (0) значительно больше, чем `BEGIN` (1). Без специальных мер модель могла бы научиться всегда предсказывать `0`, показывая при этом высокую точность (accuracy), но низкое качество на практике.

**Решение:**
*   **Взвешивание классов в функции потерь:** Перед обучением был произведен подсчет всех меток в обучающем датасете. На основе этих данных были рассчитаны веса, обратно пропорциональные частоте классов. Эти веса были переданы в функцию потерь `torch.nn.CrossEntropyLoss`. В результате ошибка на редком, но важном классе `BEGIN` "штрафовалась" сильнее, заставляя модель уделять ему больше внимания.

### 3. Оптимизация процесса обучения

Для достижения лучшего результата и стабильности обучения использовались стандартные для современных трансформеров практики.

**Решение:**
*   **Оптимизатор `AdamW`:** Стандарт де-факто для обучения трансформерных моделей.
*   **Планировщик скорости обучения (`Scheduler`):** Был применен `get_cosine_schedule_with_warmup`. Он плавно "прогревает" (увеличивает) скорость обучения на начальном этапе, а затем медленно снижает ее по косинусоиде. Это помогает модели не "проскочить" оптимальные веса и лучше сойтись.

### 4. Комплексная система оценки качества

Простая точность (accuracy) — плохая метрика для этой задачи. Поэтому была разработана система оценки, ориентированная на конечную бизнес-цель.

**Решение:**
*   **Две метрики F1-score:**
    1.  **`seqeval` (по токенам):** Стандартная метрика для Token Classification. Полезна для отладки и сравнения с другими NER-подобными задачами.
    2.  **F1 по позициям пробелов:** Ключевая метрика, которая точно отражает качество решения. Она сравнивает **множество предсказанных позиций пробелов** с множеством истинных позиций, вычисляя на их основе Precision, Recall и F1-score. Именно по этой метрике выбиралась лучшая модель.

### 5. Пост-обработка для улучшения результата

Даже идеально обученная модель расставляет только пробелы. Исходный текст мог содержать пунктуацию, которая "слипалась" в процессе удаления пробелов.

**Решение:**
*   **Гибридный подход (ML + Rules):** После предсказания модели применяется скрипт финальной обработки (`post_process_punctuation`). Этот скрипт:
    - Гарантированно ставит пробел после запятой.
    - "Интеллектуально" обрабатывает дефисы, используя словарь-исключение для сложных слов (например, `санкт-петербург`) и правила для частиц (`-то`, `-либо`, `кое-`).

## 📁 Структура проекта

*   `Training_Notebook.ipynb` (псевдоним) — ноутбук, содержащий полный цикл от подготовки данных до обучения, валидации и сохранения модели.
*   `Inference_Notebook.ipynb` (псевдоним) — ноутбук, который загружает обученную модель, выполняет предсказания на тестовых данных (`test_data.txt`), применяет пост-обработку и генерирует итоговый файл `submission.csv`.

## ▶️ Как запустить

1.  Установите необходимые зависимости (например, из `requirements.txt`).
2.  Запустите ноутбук для обучения. По завершении в указанной директории (`./rubert_tiny_spaced_restoration` или других файлах в которых будут содержаться лучшие либо по f1-score, либо по loss модельки) появятся файлы модели.
3.  Запустите ноутбук для инференса. Он создаст файл `submission.csv` с результатами.




## Способ 2: Использование посимвольного токенайзера и дообучение на таком же посимвольном BERT'e.


# Восстановление пробелов в тексте с помощью CANINE

Этот проект представляет собой решение задачи по восстановлению пропущенных пробелов в текстовых строках (например, в поисковых запросах). Для решения используется модель `google/canine-c` из библиотеки Hugging Face Transformers, которая дообучается на задаче посимвольной классификации.

Каждый символ в строке без пробелов классифицируется на один из двух классов:
1.  `SPACE`: После этого символа должен стоять пробел.
2.  `NO_SPACE`: После этого символа пробел не нужен.

## 🚀 Основные возможности

*   **Модель CANINE**: Используется `google/canine-c`, character-level модель, которая идеально подходит для задач, где важен контекст отдельных символов, а не токенов.
*   **Гибкая работа с данными**: Скрипт может работать как с базовым датасетом с Hugging Face Hub (`dgramus/synth-ecom-search-queries`), так и с дополнительными локальными данными для аугментации обучающей выборки.
*   **Надежная метрика**: Для оценки используется F1-score, вычисленный на позициях вставляемых пробелов, что точно отражает качество восстановления исходной строки.
*   **Взвешенная функция потерь**: Для борьбы с дисбалансом классов (пробелов обычно гораздо меньше, чем символов) применяется `CrossEntropyLoss` со взвешиванием.
*   **Полный пайплайн**: Ноутбук содержит весь цикл от загрузки и предобработки данных до обучения, оценки и генерации файла для отправки на соревнование.

## 📂 Структура проекта

```
.
├── approach_2.ipynb       # Основной Jupyter Notebook с кодом
├── augmented_add_train.txt       # (Опционально) Дополнительные данные для обучения
├── augmented_test_data.csv       # (Опционально) Данные для локальной валидации модели
├── test_data.txt                 # (Обязательно) Тестовые данные для генерации сабмита
├── final_corrected_model/        # Папка для сохранения лучшей модели
│   ├── config.json
│   ├── pytorch_model.bin
│   └── ...
├── submission.csv                # Итоговый файл с предсказаниями для отправки
└── README.md                     # Этот файл
```

## 🛠️ Установка

1.  Клонируйте репозиторий.
2.  Создайте виртуальное окружение.
3.  Установите все необходимые зависимости из файла `requirements.txt`.
## ⚙️ Использование

Весь процесс запускается из одного ноутбука (`approach_2.ipynb`).

### Шаг 1: Подготовка данных

1.  **Обучающие данные**:
    *   Основной датасет (`dgramus/synth-ecom-search-queries`) будет загружен автоматически из Hugging Face Hub.
    *   (Опционально) Если у вас есть дополнительные размеченные данные, сохраните их в файл `augmented_add_train.txt`. Каждая строка файла должна содержать один корректный запрос с пробелами. Скрипт автоматически обработает его для обучения.

2.  **Данные для локальной оценки**:
    *   (Опционально) Для финальной оценки на отложенной выборке создайте файл `augmented_test_data.csv` с колонками `text_no_spaces` (текст без пробелов) и `query_corrected` (правильный текст с пробелами).

3.  **Данные для сабмита**:
    *   (Обязательно) Поместите файл с тестовыми данными для конкурса в корень проекта под именем `test_data.txt`. Он должен содержать колонки `id` и `text_no_spaces`.

### Шаг 2: Запуск ноутбука

Откройте `train_and_predict.ipynb` и выполните все ячейки последовательно ("Run All").

### Что происходит во время выполнения:

1.  **Настройка**: Задаются константы (имя модели, гиперпараметры), фиксируются сиды для воспроизводимости и определяется устройство (GPU/CPU).
2.  **Загрузка данных**: Загружается базовый датасет и опционально объединяется с данными из `augmented_add_train.txt`.
3.  **Предобработка**: Текст преобразуется в формат, понятный модели CANINE, и вычисляются метки (позиции для вставки пробелов).
4.  **Обучение модели**:
    *   Модель `CanineForTokenClassification` инициализируется и отправляется на выбранное устройство.
    *   Запускается цикл обучения на `NUM_EPOCHS` эпох.
    *   После каждой эпохи модель оценивается на валидационном наборе.
    *   Лучшая модель по метрике **F1-score** сохраняется в директорию `./final_corrected_model`.
    *   Лучшая модель по метрике **validation loss** сохраняется в директорию `./final_corrected_model_loss`.
5.  **Оценка**:
    *   Лучшая модель (по F1) загружается из `./final_corrected_model`.
    *   Проводится оценка на данных из `augmented_test_data.csv` (если файл существует) и выводится итоговый F1-score.
6.  **Генерация сабмита**:
    *   Модель обрабатывает каждую строку из `test_data.txt`.
    *   Предсказанные позиции пробелов сохраняются в файл `submission.csv` в формате, готовом для отправки.

### Шаг 3: Результаты

*   **Обученная модель**: Готовая к использованию модель находится в папке `final_corrected_model/`.
*   **Файл для отправки**: Итоговый файл с предсказаниями `submission.csv` будет создан в корневой директории проекта.

## 🔧 Кастомизация

*   `MODEL_NAME`: Можно попробовать другие character-level модели.
*   `MAX_LENGTH`: Максимальная длина последовательности. Увеличение может улучшить качество на длинных строках, но замедлит обучение и потребует больше памяти.
*   `BATCH_SIZE`: Размер батча. Подбирается в зависимости от объема видеопамяти.
*   `NUM_EPOCHS`: Количество эпох обучения.
*   `LEARNING_RATE`: Скорость обучения.
